{
  "name": "prosched-frontend",
  "version": "1.0.0",
  "private": true,
  "dependencies": {},
  "scripts": {
    "start": "echo \"Start script only works locally\"",
    "build": "echo \"Build script only works locally\""
  }
}
